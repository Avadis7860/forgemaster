{
  "name": "cockpit-verify-runner",
  "private": true,
  "version": "1.0.0",
  "description": "Runner Playwright du gate Tier-1.5 (cockpit gate verify) — semé sous $COCKPIT_HOME/runners par provision-ct.sh. playwright-core seul (pas @playwright/test) ; le browser Chromium est installé à part par la CLI playwright-core.",
  "dependencies": {
    "pngjs": "^7.0.0",
    "playwright-core": "1.61.0-alpha-1781023400000"
  }
}
