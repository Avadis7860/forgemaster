"""projects — registre des projets gérés (identité durable : repo SoT + miroir + backend). Persisté en
DB (table `projects`). Concept porté de `services/aggregator/routers/projects.py`."""
from __future__ import annotations
